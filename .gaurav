export PS1=[${LOGNAME}@`uname -n`]'$PWD>\n$'
if [ -z $EMACS ]; then 
	export GREP_OPTIONS='--color=auto'
else
	export TERM='xterm-color'
fi
. ~/.aliases

export JAVA_HOME=$(/usr/libexec/java_home)

export MAVEN_HOME=/Users/gabbi/softwares/apache-maven-3.1.1

export postGresPath=/Users/gabbi/softwares/postgres/Postgres.app/Contents/MacOS/bin

export GRADLE_HOME=/Users/gabbi/softwares/gradle-1.10

export PLAY_PATH=/Users/gabbi/softwares/activator-1.3.7-minimal

export PATH=$PATH:$MAVEN_HOME/bin:$postGresPath:$PLAY_PATH:/usr/texbin

if [ -z $EMACS ]; then
     	echo "not in emacs"
else
	export PATH=$PATH:/usr/local/bin     
fi

shopt -s nocaseglob

export HISTSIZE=5000

export EDITOR="emacs -nw"
